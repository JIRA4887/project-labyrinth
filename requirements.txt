# Project Labyrinth — Dependencies
# Install with: pip install -r requirements.txt
# Or:           pip install -e .

# ── Core (required on all Python versions) ────────────────────────────────────
sentence-transformers>=2.7.0   # Semantic encoder (all-MiniLM-L6-v2)
tiktoken>=0.7.0                # OpenAI-compatible token counting
numpy>=1.26.0                  # Embedding math + NumpyL3Backend (built-in L3)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Optional: ChromaDB L3 backend (Python <= 3.12 only) ──────────────────────
# On Python >= 3.13, the built-in NumpyL3Backend is used automatically.
# Only install chromadb if you need persistent cross-session storage on Py 3.11/3.12.
# chromadb>=0.5.0

# ── Optional: LLM Integration ─────────────────────────────────────────────────
# openai>=1.30.0               # Uncomment to use with OpenAI API
# anthropic>=0.28.0            # Uncomment to use with Anthropic Claude
